/**
 * Regenerate with API running: npm run openapi
 * See docs/DASHBOARD_DEPLOY.md
 */
export interface paths {
  '/health': { get: { responses: { 200: { content: { 'application/json': unknown } } } } }
  '/ready': { get: { responses: { 200: { content: { 'application/json': unknown } } } } }
}
